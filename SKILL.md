---
name: pptskill
description: 创建、修改和检查可编辑的 PowerPoint（.pptx），将资料整理成有叙事节奏的中文演示稿。支持按时长分镜、视觉风格预设、模板保留和逐页检查；仅需大纲时不擅自生成文件。
---

# PPTSKILL

将用户的主题、资料或现有演示稿转为适合受众的演示内容，默认交付可编辑的 `.pptx`。用户明确只需大纲、讲稿、审阅意见或其他格式时，按其要求交付。

## 判断任务与输入

- 从上下文提取受众、目的、页数或时长、语言、使用场景、素材及交付格式。已经明确的信息不重复询问。
- 缺少主题、关键材料或会改变结论的数据时，提出具体问题；同时完成不依赖答案的资料整理。风格未指定时可采用简洁的商务风格，并简短说明假设。
- 区分模板与内容来源。要求沿用模板时保留设计；仅提供资料时不自动照搬其设计。
- 指定 N 页就是总计 N 页，封面和结束页计入总数，除非用户另有说明。一页任务不另加封面。没有页数要求时，按信息量安排，不固定套用十页结构。
- 修改已有文件时先创建工作副本。只读分析不另存改稿。

## 组织内容

复杂主题、指定讲述时长或强调说服力时，采用 [Director Mode](references/director.md) 规划每页目的、证据、视觉和用时。普通改字直接处理，不强制启动完整分镜流程。

先在工作目录建立简短的逐页计划：页码、主题或结论、证据、呈现方式。简单修改无需额外制作计划文件，也不要求用户批准每个阶段。

每页围绕一个明确目的组织内容。介绍概念可使用主题标题，汇报发现可使用有证据支持的结论标题。删除空泛口号、重复副标题和为了填满页面而增加的段落。根据任务需要选择叙事结构，不强制附加目录、建议、致谢等页面。

数据记录单位、时间范围、口径和来源。保留影响结论的限制条件；缺失数据标注“待补充”或使用明确标记的演示数据，不编造企业业绩、引用和统计结果。涉及最新信息时验证来源与日期。

外部事实和图片来源放在对应页的备注或用户指定位置，需要观众看到的限定条件放在页面内。讲者备注补充解释，不放制作日志和校验过程。

## 设计与实现

创建或重排页面时阅读 [版式与中文排版](references/design.md)。修改模板时先检查其母版、布局、字体、图表、备注和页面顺序，再实施最小范围修改。

没有既定模板，或用户要求新的视觉方向时，从 [Design DNA](references/style-presets.md) 选择适合内容的预设。用户指定 `midnight`、`editorial` 或 `blueprint` 时读取相应 JSON。它们提供设计参数，不是固定本机依赖或已安装的 PowerPoint 母版。

检查当前环境实际可用的演示工具和渲染器，阅读所选工具的当前文档。已有演示工具或专用工作流可满足任务时优先使用；不要假设某个商业插件、固定本机路径或库已经安装。没有可用生成工具时说明具体缺口，保留已完成内容，不将 Markdown 或图片改扩展名伪装成 PPTX。

- 保持标题、正文、表格和必要的数据图表可编辑。图片用于照片、插图与装饰，不能替代要求编辑的整页内容。
- 图表采用适合问题的形式，保留可编辑数据、单位、轴标签及必要的基线。精简表格时不删除用户要求保留的行列。
- 使用用户原始 Logo 和必须保持一致的素材。按比例缩放，检查裁切区域。
- 文本溢出时先精简措辞或调整布局。在页数限制允许时再拆页，避免一味缩小字体。
- 对复杂模板谨慎选择编辑工具。往返转换可能影响动画、SmartArt、链接、母版或嵌入对象，无法保真时明确说明影响。

## 检查与交付

按 [交付前检查](references/quality-check.md) 核对内容与视觉效果。导出后重新打开文件，并将每页渲染为图片或 PDF 逐页查看；修正错字、溢出、裁切、缺字、遮挡和对齐问题，再检查受影响的页面。

需要检查 `.pptx` 页数、文本、备注和原生对象数量时，运行随附的只读脚本。将下面路径替换为 skill 的实际目录及输入文件路径：

```bash
python3 /path/to/pptskill/scripts/inspect_pptx.py /path/to/deck.pptx > /path/to/work/deck-inspection.json
```

脚本只读取文件，不渲染，也不能判定视觉质量、文本溢出或所有内容是否可编辑。无法使用渲染器时完成结构与内容检查，并明确告知尚未验证页面外观。仅在实际打开并查看 PowerPoint 后才声称完成 PowerPoint 验证。

需要页数约束、候选占位文本、文字密度等可执行检查时，使用 [Deck Doctor](references/deck-doctor.md)：

```bash
python3 /path/to/pptskill/scripts/deck_doctor.py /path/to/deck.pptx --expected-slides 8
```

只在用户确实要求 8 页时传入该值。逐条解释诊断证据，对教学示例中的占位词、全屏照片和重复品牌文字保留人工判断，不机械删除内容。不以无告警或某个分数宣称设计优秀。

提供最终文件链接和影响使用的限制。PDF、图片、源代码或额外版本仅在用户要求或确有交付必要时附上。临时脚本、素材缓存和校验报告留在工作目录。此 skill 不授权向 GitHub、网盘或其他服务发布用户的演示材料。
